public main(console: mut system.console.Console) -> void
{
    var s: string = "A good test string!";
    console.WriteLine(string_length(s) + ": " + s);
    s = "Yep,\twe know length!";
    console.WriteLine(string_length(s) + ": " + s);
}
