public Main(console: mut System.Console.Console) -> void
{
	let l: mut System.Collections.List<string> = new System.Collections.List<string>();
	l.Add("Hello");
	l.Add("World!");
	// TODO Length should be a property not a method
	if l.Length() <> 2 // Collections of contiguous elements have length that replaces the Count property in the interface
	{
		console.WriteLine("List length not 2!");
	}

	console.WriteLine(l.Get(0)); // TODO this should be an indexer
	for let s: string in l
	{
		console.Write(s);
	}
	console.WriteLine();
}
