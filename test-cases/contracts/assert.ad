public main() -> void
{
	let x: int = 5;
	let y: int = 5;
	let z: int = 6;

	assert(x == y, "x="+int_to_string(x)+" y="+int_to_string(y));

	assert(y == z, "y="+int_to_string(y)+" z="+int_to_string(z));
}
