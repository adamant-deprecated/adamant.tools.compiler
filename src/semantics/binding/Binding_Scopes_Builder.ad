/// Builds the binding scopes so that we can look up names
public class Binding_Scopes_Builder
{
	public build(self,
		package_name: Package_Name,
		package_syntax: Syntax_Node,
		references: System.Collections.List<Package_Reference>)
		-> Binding_Scope
	{
		// TODO load references into `global_binding_scope`
		// TODO load package declarations into `global_binding_scope`
		NOT_IMPLEMENTED("");
	}
}
